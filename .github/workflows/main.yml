name: Fast CI

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  fast-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Quick Repo Verify
        run: echo "Butt Network Code Pushed Successfully!"
